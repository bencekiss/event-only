<h1>Event in <%= @location.city %></h1>
<p><%= link_to 'Go to events in this city', location_events_path %></p>

<p>
  <%= link_to 'Edit', edit_location_event_path %>
   | <%= link_to 'Delete', location_event_path, method: :delete, data: {confirm: "Are you sure you want to delete the event at '#{@location.name}'? This cannot be undone!"} %>
</p>
<%= render 'event', event: @event %>
